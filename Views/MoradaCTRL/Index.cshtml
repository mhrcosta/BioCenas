@model IEnumerable<BioCenas.Models.MoradaMDL>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Morada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodigoPostal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Morada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoPostal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Morada">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Morada">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Morada">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
