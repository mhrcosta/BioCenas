@model IEnumerable<BioCenas.Models.EncomendaMDL>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_DescritivoEncomenda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_LocalEntrega)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataEntrega)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_DescritivoEncomenda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_LocalEntrega)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataEntrega)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Encomenda">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Encomenda">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Encomenda">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
